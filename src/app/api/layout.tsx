export default function ApiLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
      <div>
        {/* Add common layout elements for API demo pages */}
        {children}
      </div>
    )
  }
  
  